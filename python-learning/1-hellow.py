name = input('你好') # input可以让用户手输字符串，并存放到变量里
print(name + 'world!')

#  Python数据类型：哈希类型、不可哈希类型
 # 哈希类型，即在原地不能改变的变量类型，不可变类型。可利用hash函数查看其hash值，也可以作为字典的key
int,float,complex # 数字类型
str,bytes # 字符串类型
tuple # 元组类型
frozenset # 冻结集合
True,False # 布尔类型
None # 空类型
list,set,dict # 不可哈希类型，原地可变，不能作为字典的key
print('the quick bronw fox', 'jumps over', 'the lazy dog')

name2 = input()